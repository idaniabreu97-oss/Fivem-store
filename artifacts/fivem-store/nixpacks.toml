[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
cmds = [
  "npm install -g pnpm",
  "cd ../.. && pnpm install --frozen-lockfile"
]

[phases.build]
cmds = [
  "cd ../.. && NODE_ENV=production pnpm --filter @workspace/fivem-store run build"
]

[start]
cmd = "npx serve dist/public -l $PORT"
